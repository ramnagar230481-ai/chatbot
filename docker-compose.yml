version: "3.9"

services:
  chatbot:
    build: .
    container_name: dialogpt-chatbot
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - model-cache:/root/.cache/huggingface
    restart: unless-stopped
    mem_limit: 4g

volumes:
  model-cache:
